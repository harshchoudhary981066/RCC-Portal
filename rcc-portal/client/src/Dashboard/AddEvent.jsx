import React from "react";

const AddEvent = () => {
  return (
    <section className="panel-view">
      <h2 className="panel-view-header">Add an event</h2>
    </section>
  );
};

export default AddEvent;
